# Auto-update files (free via GitHub)

Put release assets `base_pack.zip` and `catalog.zip` on a GitHub Release
(e.g. tag `packs-v1`), then bump numbers in `version.json`.

Players do not need to redownload the mod JAR.
